# Gulp Сборка: Проект "gulpmy"

Данный проект выполнен в рамках учебного задания для закрепления навыков автоматизации фронтенд-разработки.

## 🎯 Цели задания
*   **Закрепить знания:** Глубокое понимание принципов работы таск-менеджера Gulp.
*   **Отработка навыков:** Создание полноценной конфигурации Gulp-сборки «с нуля».
*   **Разделение окружений:** Реализация разных сценариев сборки для разработки (`dev`) и для финального продакшена (`build`).

---

## 🛠 Что реализовано в сборке

### 1. Обработка стилей (CSS)
*   Объединение всех файлов в один `main.css` (**concat**).
*   Автоматическое добавление вендорных префиксов (**autoprefixer**).
*   Минификация (сжатие) CSS для продакшн-версии (**clean-css**).
*   Поддержка карт исходного кода (**sourcemaps**) в режиме разработки.

### 2. Обработка JavaScript
*   Транспиляция кода из современных стандартов в старые с помощью **Babel** (для поддержки старых браузеров).
*   Объединение JS-модулей в единый файл `app.js`.
*   Минификация кода через **uglify-es** (только в финальной сборке).
*   Вывод уведомлений об ошибках через **gulp-notify**.

### 3. Работа с графикой и ресурсами
*   Создание **SVG-спрайтов** (режим stack).
*   Оптимизация и сжатие изображений (JPG, PNG, SVG).
*   Перенос статических файлов (шрифты, доп. ресурсы) в папку `dist`.

### 4. Автоматизация и сервер
*   Живая перезагрузка страницы при изменении кода с помощью **Browser-sync**.
*   Автоматическое отслеживание изменений (**watch**) во всех рабочих директориях.
*   Очистка папки `dist` перед каждой новой сборкой.

---

## 📦 Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Rrom-Ka/gulpmy.git

## 🚀 Инструкция по запуску

### Предварительные требования
Убедитесь, что у вас установлены [Node.js](https://nodejs.org) и пакетный менеджер npm.

### Установка зависимостей
```bash
npm install
